# Project Rules for /aif-fix

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-06-07 13:30
> Based on: 4 analyzed patches

## Rules

### Check for data-dependent UI controls
**Source**: 2026-06-06-19.48.md
**Rule**: When fixing React components, verify that action buttons and interactive controls are NOT nested inside conditional data blocks (`{data && ...}`). Use `{!isLoading && ...}` instead, so controls render independently of data arrival.

### Verify API client usage
**Source**: 2026-06-06-19.48.md
**Rule**: When fixing frontend API bugs, check that ALL API calls use the shared `request()` function from `api/client.ts`. Direct `fetch()` calls in components are an automatic red flag — they will fail due to missing Telegram initData auth.

### Add Cache-Control to API responses
**Source**: 2026-06-06-19.48.md
**Rule**: When fixing backend API endpoints, add `Cache-Control: no-store` header to all Gin responses to prevent stale cached data from reaching the client.

### Await SDK async functions
**Source**: 2026-06-07-08.00.md
**Rule**: When fixing Telegram SDK integration issues, verify that all SDK functions returning Promises (`isTMA()` etc.) are called with `await`. Unawaited Promises always appear truthy, causing conditional branches to never execute.

### Call initData.restore() after init()
**Source**: 2026-06-07-08.00.md
**Rule**: After calling `init()` from `@telegram-apps/sdk`, always call `initData.restore()`. Without it, `initData.raw()`, `initData.user()`, and related methods return `null`/`undefined`.

### Handle 429 rate-limit errors on frontend
**Source**: 2026-06-07-08.00.md
**Rule**: When debugging frontend API errors, check if the error is `ApiError` with status 429. If so, extract `daily_used`, `daily_limit`, `is_premium` from `err.data` and update the usage state accordingly.

### Add fallback for AI streaming
**Source**: 2026-06-07-10.45.md
**Rule**: When fixing AI integration bugs, ensure all streaming methods have a fallback when the primary AI provider fails (Gemini 429, etc.). Extract only the `reply` field from fallback JSON for token display.

### Use double braces for i18n interpolation
**Source**: 2026-06-07-10.45.md
**Rule**: When fixing i18n translation issues, verify that all interpolation variables use `{{variable}}` (double braces), not `{variable}` (single braces). i18next/react-i18next requires double braces.

### Read WebApp URL from env
**Source**: 2026-06-07-10.45.md
**Rule**: When fixing bot URL issues, ensure WebApp URLs are read from environment variables, never hardcoded. Pass the URL as a parameter through the call chain.

### Account for NavBar height in padding
**Source**: 2026-06-07-10.45.md
**Rule**: When fixing UI components near bottom navigation, use `paddingBottom: 'calc(var(--nav-height) + var(--safe-bottom))'` to prevent content from being hidden behind the NavBar.

### Map language codes to full names in AI prompts
**Source**: 2026-06-07-10.50.md
**Rule**: When fixing AI prompt language issues, always map two-letter codes (`uz` → `Uzbek`, `ru` → `Russian`) to full language names. Place the language instruction at the top of the prompt, before any rules, without contradictions.

### Check Go slices for nil before JSON serialization
**Source**: 2026-06-07-10.50.md
**Rule**: When fixing backend handlers that return slices, check for nil before JSON serialization: `if entries == nil { entries = []T{} }`. Go serializes nil slices as `null` in JSON, which can crash frontend code accessing `.length`.

### Set default [] for React data array props
**Source**: 2026-06-07-10.50.md
**Rule**: When fixing React components that receive `data: T[]` props, always provide a default empty array: `function Comp({ data = [], ... })`. This prevents crashes from accessing `.length` or `.map()` on `null`/`undefined`.

### Use initData.raw() from SDK only
**Source**: 2026-06-07-10.50.md
**Rule**: When fixing auth or initData issues, verify all API requests use `initData.raw()` from `@telegram-apps/sdk`. Never use `(window as any).Telegram?.WebApp?.initData` — it is undefined in dev mode with mock Telegram environment.
