# Project Rules for /aif-implement

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-06-07 13:30
> Based on: 4 analyzed patches

## Rules

### Render UI skeleton before data
**Source**: 2026-06-06-19.48.md
**Rule**: When implementing React components, render all structural elements (buttons, layouts, navigation) first using `{!isLoading && ...}` guards. Interactive controls must NOT depend on data availability.

### Use shared API client for all requests
**Source**: 2026-06-06-19.48.md
**Rule**: When implementing frontend features with API calls, always use the exported functions from `api/client.ts` (e.g., `getProgress()`, `getLevelTestQuestions()`). Never use bare `fetch()` in components.

### Set Cache-Control on new API endpoints
**Source**: 2026-06-06-19.48.md
**Rule**: When creating new backend API handlers, add `Cache-Control: no-store` to the response header using `c.Header("Cache-Control", "no-store")`.

### Configure Vite proxy for dev mode
**Source**: 2026-06-07-08.00.md
**Rule**: When implementing features that require API access, ensure the Vite dev server has a proxy configured for `/api` → backend URL. Without it, requests go to the Vite port instead of the Go server.

### Add AI streaming fallback
**Source**: 2026-06-07-10.45.md
**Rule**: When implementing methods that call external AI services in streaming mode, always include a fallback to a non-streaming fallback provider. Parse fallback JSON responses to extract only the display text (e.g., `reply` field).

### Use double braces for i18n interpolation keys
**Source**: 2026-06-07-10.45.md
**Rule**: When adding new i18n translation keys, use `{{variable}}` (double braces) for interpolation. Single braces `{variable}` are silently ignored by i18next.

### Read WebApp URL from env
**Source**: 2026-06-07-10.45.md
**Rule**: When implementing bot or WebApp features, read URLs from environment variables. Never hardcode URLs — pass them as parameters through the call chain.

### Account for NavBar in padding
**Source**: 2026-06-07-10.45.md
**Rule**: When implementing UI components positioned near the bottom of the screen, add `paddingBottom: 'calc(var(--nav-height) + var(--safe-bottom))'` to prevent overlap with the fixed NavBar.

### Map AI prompt language codes to full names
**Source**: 2026-06-07-10.50.md
**Rule**: When implementing or modifying AI prompts that handle language, map language codes (`uz` → `Uzbek`, `ru` → `Russian`) to full names. Place the language instruction prominently at the top of the prompt.

### Check Go nil slices before JSON return
**Source**: 2026-06-07-10.50.md
**Rule**: When implementing backend handlers that return slices via JSON, add a nil check: `if x == nil { x = []T{} }`. Go serializes nil slices as JSON `null`, which can crash frontend consumers.

### Use SDK initData for all API auth
**Source**: 2026-06-07-10.50.md
**Rule**: When implementing frontend features that make API calls, always pass initData via `initData.raw()` from `@telegram-apps/sdk`. Never use `window.Telegram.WebApp.initData`.
